const fontTitleOptions = {
  family: 'Syne',
  size: 12,
};

const fontOptions = {
  size: 9,
};

const axisOptions = {
  ticks: {
    font: fontOptions,
  },
  stacked: true,
};

export { axisOptions, fontTitleOptions };
